
- name: Stop netbird service
  become: true
  ansible.builtin.systemd:
    name: netbird
    state: stopped
    enabled: false

- name: Remove Netbird config file
  become: true
  ansible.builtin.file:
    path: /etc/netbird/config.json
    state: absent

- name: Remove Netbird and WireGuard packages
  become: true
  package:
    name:
      - wireguard
      - netbird={{ netbird_version }}
    state: absent
# /var/snap/microk8s/current/args/certs.d/docker.io/hosts.toml
server = "https://{{ registry_mirror_fqdn }}:{{ registry_mirror_port }}"

[host."{{ registry_mirror_fqdn }}:{{ registry_mirror_port }}"]
capabilities = ["pull", "resolve"]
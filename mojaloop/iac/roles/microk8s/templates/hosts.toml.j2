server = "https://{{ item }}"

[host."https://{{ registry_mirror_fqdn }}"]
capabilities = ["pull", "resolve"]
authorization = "Basic {{ (docker_registry_username + ':' + docker_registry_password) | b64encode }}"
skip_verify = false
override_path = true